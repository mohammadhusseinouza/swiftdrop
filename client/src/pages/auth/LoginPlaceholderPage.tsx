import RoutePlaceholder from '../RoutePlaceholder';

/** Real Login Page: Phase 11.1. */
export default function LoginPlaceholderPage() {
  return <RoutePlaceholder section="Auth" title="Login" phase="Phase 11.1" />;
}
