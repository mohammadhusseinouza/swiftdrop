import RoutePlaceholder from '../RoutePlaceholder';

/** Real Public Tracking UI + safe backend DTO: Phase 14. No API call here. */
export default function TrackingPlaceholderPage() {
  return (
    <RoutePlaceholder section="Public" title="Order Tracking" phase="Phase 14" />
  );
}
